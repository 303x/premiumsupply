
import './globals.css';

export const metadata = {
  title: 'PREMIUM PEPTIDES',
  description: 'Private referral-only member access'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
